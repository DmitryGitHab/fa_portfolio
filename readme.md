Сайт - визитка на FastApi, с использваонием CSS и JavaScript для создания/изменения/удаления проектов.

Для установки зависимостей:
pip install -r req.txt

для запуска сервера:
uvicorn main:app --reload --port 8040